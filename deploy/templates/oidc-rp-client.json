{
  "$comment": [
    "OIDC relying-party (RP) client registration template.",
    "cwl-idp is the OIDC PROVIDER; each ecosystem app is an RP. Register one",
    "project + one app per RP via the ZITADEL Management API:",
    "  POST /management/v1/projects                        (once per RP)",
    "  POST /management/v1/projects/{projectId}/apps/oidc  (this body)",
    "",
    "The returned clientId/clientSecret are stored in the IdP DB by ZITADEL and",
    "mirrored into the ecosystem KV under secret/idp/rp/<name> — NOT in any RP",
    "env file. RPs read their client secret from KV at boot. See",
    "docs/rp-onboarding.md. Known RPs: naruon, pg-erd-cloud,",
    "semantic-data-portal, clearfolio, contextual-orchestrator, newsdom-api."
  ],

  "name": "{{rp_name}}",

  "redirectUris": ["{{rp_redirect_uri}}"],
  "$redirect_example": "https://naruon.example/auth/callback",
  "postLogoutRedirectUris": ["{{rp_post_logout_uri}}"],

  "responseTypes": ["OIDC_RESPONSE_TYPE_CODE"],
  "grantTypes": ["OIDC_GRANT_TYPE_AUTHORIZATION_CODE", "OIDC_GRANT_TYPE_REFRESH_TOKEN"],

  "appType": "OIDC_APP_TYPE_WEB",

  "$auth_method_notes": [
    "Confidential web/service RPs: BASIC or JWT (private_key_jwt preferred).",
    "SPA/native RPs behind the WAF: set appType NATIVE/USER_AGENT and use",
    "PKCE with authMethodType OIDC_AUTH_METHOD_TYPE_NONE."
  ],
  "authMethodType": "OIDC_AUTH_METHOD_TYPE_PRIVATE_KEY_JWT",

  "devMode": false,
  "accessTokenType": "OIDC_TOKEN_TYPE_JWT",
  "accessTokenRoleAssertion": true,
  "idTokenRoleAssertion": true,
  "idTokenUserinfoAssertion": true,
  "clockSkew": "5s",

  "additionalOrigins": []
}
